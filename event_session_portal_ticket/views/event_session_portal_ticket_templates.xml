<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List of my events -->
    <template id="portal_my_events_tickets_inherit" inherit_id="event_portal_ticket.portal_my_events_tickets">
        <xpath expr="//tbody/tr/td[1]" position="replace">
            <td>
                <t t-if="not event.use_sessions">
                    <a t-attf-href="/my/tickets/event/#{event.id}">
                        <span t-field="event.name"/>
                    </a>
                </t>
                <t t-else="">
                    <a t-attf-href="/my/tickets/event/sessions/#{event.id}">
                        <span t-field="event.name"/>
                    </a>
                </t>
            </td>
        </xpath>

        <xpath expr="//tbody/tr/td[3]" position="replace">
            <td>
                <t t-if="not event.use_sessions">
                    <t>
                        <t t-set="tickets" t-value="event.get_current_user_tickets_count(event.id)"/>
                        <span class="badge" t-attf-style="background-color: var(--o-cc1-btn-primary); padding-right: .6em; padding-left: .6em; border-radius: 10rem;">
                            <span class="fa fa-ticket"/>
                                <t t-esc="tickets"/>
                                <t t-if="tickets &gt; 1">tickets</t>
                                <t t-else="">
                                    ticket
                                </t>
                        </span>
                    </t>
                </t>
                <t t-else="">
                    <t t-set="sessions" t-value="event.get_current_user_sessions(event.id)"/>
                    <t t-set='sessions_count' t-value='len(sessions)'/>
                    <t t-if="sessions_count == 1">
                        <t t-set="participants_count" t-value="len(event.get_sessions_registrations(sessions))"/>
                        <span class="badge" t-attf-style="background-color: var(--o-cc1-btn-primary); padding-right: .6em; padding-left: .6em; border-radius: 10rem;">
                            <span class="fa fa-ticket"></span>
                            <span><t t-esc="participants_count"/> 
                            <t t-if="participants_count &gt; 1">billets</t>
                                <t t-else="">
                                    billet
                                </t>
                            </span>
                        </span>
                    </t>
                    <t t-else="">
                    <span class="badge" t-attf-style="background-color: var(--o-cc1-btn-primary); padding-right: .6em; padding-left: .6em; border-radius: 10rem;">
                        <span class="fa fa-calendar"></span>
                        <span>
                            <span><t t-esc="sessions_count"/> 
                            <t t-if="sessions_count &gt; 1">séances</t>
                                <t t-else="">
                                    séance
                                </t>
                            </span>
                        </span>
                    </span>
                    </t>
                </t>
            </td>
        </xpath>
    </template>

    <!-- Session details -->
    <template id="portal_my_event_sessions" name="My Tickets">
        <t t-call="portal.portal_layout" groups="base.group_portal,event.group_event_user, event.group_event_manager">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Sessions</t>
            </t>
            <t t-if="not sessions">
                <div class="alert alert-warning mt8" role="alert">
                    There are no sessions.
                </div>
            </t>
            <t t-if="sessions" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Event</th>
                        <th>Event Date</th>
                        <th>Informations</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="sessions" t-as="session">
                        <td>
                            <a t-attf-href="/my/tickets/event/session/#{session.id}">
                                <span t-field="session.name"/>
                            </a>
                        </td>
                        <td>
                            <strong>
                                <span t-field="session.date_begin" t-options='{"format": "d MMM yyyy"}'/>
                            </strong>
                            at
                            <span t-field="session.date_begin" t-options='{"format": "H:mm"}'/>
                        </td>
                        <td>
                            <t t-set="participants_count" t-value="session.get_participants_count()"/>
                            <span class="badge" t-attf-style="background-color: var(--o-cc1-btn-primary);padding-right: .6em; padding-left: .6em; border-radius: 10rem;">
                                <span class="fa fa-ticket"></span>
                                <span><t t-esc="participants_count"/>
                                    <t t-if="participants_count &gt; 1">billets</t>
                                    <t t-else="">
                                        billet
                                    </t>
                                </span>
                            </span>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <!-- Session ticket view -->
    <template id="portal_my_event_session_tickets" name="My Tickets" inherit_id="portal.portal_sidebar" primary="True">
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <t t-set="o_portal_fullwidth_alert" groups="base.group_portal,event.group_event_user, event.group_event_manager">
                <t t-call="portal.portal_back_in_edit_mode">
                </t>
            </t>

            <div class="row mt16 o_portal_event_sidebar">
                <!-- Sidebar -->
                <t t-call="portal.portal_record_sidebar">
                    <t t-set="classes" t-value="'col-lg-auto d-print-none'"/>
                    <t t-set="title">
                        <div class="text-center pb-2 pt-3">
                            <h2 class="mb-3">
                                <i class="fa fa-ticket"/>

                                <b t-esc="len(registrations)"/>
                                <br/>
                                <strong t-if="len(registrations) &gt; 1">Registrations</strong>
                                <strong t-else="">Registration</strong>
                            </h2>
                        </div>
                    </t>
                    <t t-set="entries">
                        <ul class="list-group list-group-flush flex-wrap flex-row flex-lg-column">
                            <li class="list-group-item flex-grow-1">
                                <div class="o_download_pdf btn-toolbar flex-sm-nowrap">
                                    <div class="btn-group  flex-grow-1 mr-1 mb-1">
                                        <a class="btn btn-primary btn-block o_download_btn"
                                           title="Download"
                                           t-attf-href="/report/pdf/event.event_registration_report_template_foldable_badge/#{registration_ids}"
                                           download="">
                                            <i class="fa fa-download"/>
                                            Download
                                        </a>
                                    </div>
                                    <div class="btn-group flex-grow-1 mb-1" t-attf-style="margin-left: 4px">
                                        <a class="btn btn-primary btn-block o_print_btn o_portal_event_print bg-o-color-1"
                                           id="print_event_report" title="Print"
                                           t-attf-href="/report/pdf/event.event_registration_report_template_foldable_badge/#{registration_ids}"
                                        >
                                            <i class="fa fa-print"/>
                                            Print
                                        </a>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </t>
                </t>

                <!-- Page Content -->
                <div id="event_content" class="col-12 col-lg">
                    <div class="o_portal_html_view shadow p-3">
                        <div>
                            <t t-call="event_session_portal_ticket.content" groups="base.group_portal,event.group_event_user, event.group_event_manager">
                                <t t-set="docs" t-value="session"/>
                            </t>
                        </div>
                    </div>
                    <br/>

                    <!--QR code-->
                </div>
            </div>
        </xpath>
    </template>

    <!--        Breadcrumb-->
    <template id="portal_layout_session_ticket" name="Portal layout: event menu entry" inherit_id="portal.portal_breadcrumbs"
              priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
        
            <t t-if="page_name == 'sessions'">
                <li t-if="sessions"
                    t-attf-class="breadcrumb-item #{'active ' if not sessions else ''}">
                    <span t-field="sessions.event_id.name"/>
                </li>
                
                
                <!-- <li class="col-lg-2"
                    t-attf-class="breadcrumb-item #{'active ' if not event else ''}">
                    <a t-if="event" t-attf-href="/my/tickets/event/session?{{ keep_query() }}">Ticket(s)</a>
                    <t t-else="">Ticket(s)</t>
                </li>
                <li t-if="session"
                    t-attf-class="breadcrumb-item #{'active ' if not session else ''}">
                    <a t-attf-href="/my/tickets/event/sessions/{{ session.event_id.id }}">
                        <span t-field="session.name"/>
                    </a>
                </li>
                <li t-if="session"
                    t-attf-class="breadcrumb-item #{'active ' if not session else ''}">
                    <span t-field="session.name"/>
                    <span t-field="session.date_begin" t-options='{"format": "d MMM yyyy"}'/>
                </li> -->
            </t>
        </xpath>
    </template>
</odoo>
