<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--    List of all events for user-->
    <template id="portal_my_home" name="Show Tickets" customize_show="True" inherit_id="portal.portal_my_home"
              priority="60">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Tickets</t>
                <t t-set="url" t-value="'/my/tickets'"/>
                <t t-set="placeholder_count" t-value="'ticket_count'"/>
            </t>
        </xpath>
    </template>

    <!--    List of my events-->
    <template id="portal_my_events_tickets" name="My Events">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Ticket(s)</t>
            </t>
            <t t-if="not events">
                <div class="alert alert-warning mt8" role="alert">
                    There are no events.
                </div>
            </t>
            <t t-if="events" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Event</th>
                        <th>Event Date</th>
                        <th>Informations</th>
                    </tr>
                </thead>
                <tbody>
                    <tr t-foreach="events" t-as="event">
                        <td>
                            <t>
                                <a t-attf-href="/my/tickets/events/#{event.id}">
                                    <span t-field="event.name"/>
                                </a>
                            </t>
                        </td>
                        <td>
                            <t>
                                <strong>
                                    <span t-field="event.date_begin" t-options='{"format": "d MMM yyyy"}'/>
                                </strong>
                                at
                                <span t-field="event.date_begin" t-options='{"format": "hh:mm"}'/>
                            </t>
                        </td>
                        <td>
                            <t t-if="not event.use_sessions">
                                <t t-set="tickets" t-value="event.get_current_user_tickets_count(event.id)"/>
                                <span class="badge badge-pill badge-primary">
                                    <i class="fa fa-ticket"></i>
                                    <span t-out="tickets"/>
                                    <t t-if="tickets &gt; 1">tickets</t>
                                    <t t-else="">
                                        ticket
                                    </t>
                                </span>
                            </t>
                            <t t-else="">
                                <span class="badge badge-pill badge-secondary">
                                    <i class="fa fa-clock-o"></i>
                                    <span t-out="len(event.get_current_user_sessions(event.id))"/>
                                    <t t-if="len(event.get_current_user_sessions(event.id)) &gt; 1">sessions</t>
                                    <t t-else="">
                                        session
                                    </t>
                                </span>
                            </t>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <!--    Breadcrumb -->
    <template id="portal_layout" name="Portal layout: event menu entry" inherit_id="portal.portal_breadcrumbs"
              priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'events' or event" class="col-lg-2"
                t-attf-class="breadcrumb-item #{'active ' if not event else ''}">
                <a t-if="event" t-attf-href="/my/tickets?{{ keep_query() }}">Ticket(s)</a>
                <t t-else="">Ticket(s)</t>
            </li>
            <li t-if="event" class="breadcrumb-item active">
                <t t-out="event.name"/>
            </li>
        </xpath>
    </template>


    <template id="portal_my_event_tickets" name="My Event" inherit_id="portal.portal_sidebar" primary="True">
        <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
            <t t-set="o_portal_fullwidth_alert" groups="event.group_event_user">
                <t t-call="portal.portal_back_in_edit_mode">
                </t>
            </t>

            <div class="row mt16 o_portal_event_sidebar">
                <!-- Sidebar -->
                <t t-call="portal.portal_record_sidebar">
                    <t t-set="classes" t-value="'col-lg-auto d-print-none'"/>
                    <t t-set="title">
                        <div class="text-center pb-2 pt-3">
                            <h2 class="mb-3">
                                <i class="fa fa-ticket"/>
                                <b t-out="len(registrations)"/>
                                <br/>
                                <strong t-if="len(registrations) &gt; 1">Registrations</strong>
                                <strong t-else="">Registration</strong>
                            </h2>
                        </div>
                    </t>
                    <t t-set="entries">
                        <ul class="list-group list-group-flush flex-wrap flex-row flex-lg-column">
                            <li class="list-group-item flex-grow-1">
                                <div class="o_download_pdf btn-toolbar flex-sm-nowrap">
                                    <div class="btn-group  flex-grow-1 mr-1 mb-1">
                                        <a class="btn btn-primary btn-block o_download_btn"
                                           title="Download"
                                           t-attf-href="/report/pdf/event.event_registration_report_template_foldable_badge/#{registration_ids}"
                                           download="">
                                            <i class="fa fa-download"/>
                                            Download
                                        </a>
                                    </div>
                                    <div class="btn-group flex-grow-1 mb-1">
                                        <a class="btn btn-primary btn-block o_print_btn o_portal_event_print bg-o-color-1"
                                           id="print_event_report" title="Print"
                                           t-attf-href="/report/pdf/event.event_registration_report_template_foldable_badge/#{registration_ids}"
                                        >
                                            <i class="fa fa-print"/>
                                            Print
                                        </a>
                                    </div>
                                </div>
                            </li>
                        </ul>
                    </t>
                </t>

                <!-- Page Content -->
                <div id="event_content" class="col-12 col-lg">
                    <div class="o_portal_html_view shadow p-3">
                        <div>
                            <t t-call="event_portal_ticket.content">
                                <t t-set="docs" t-value="event"/>
                            </t>
                        </div>
                    </div>
                    <br/>

                    <!--QR code-->
                </div>
            </div>
        </xpath>
    </template>


</odoo>
